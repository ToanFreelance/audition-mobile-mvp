name: S1.2R Cleanup Bright Stage Staging

on:
  push:
    branches:
      - work/s1-2r-bright-stage-reset
    paths:
      - .github/workflows/s1-2r-pull-bright-stage.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Remove verified temporary Bright Stage candidate
        run: |
          set -euo pipefail
          curl --fail-with-body -sS \
            -H 'content-type: application/json' \
            -d '{"op":"cleanup"}' \
            https://uaosdkrfxidiwqljmelg.supabase.co/functions/v1/p37-stage-bright-v1-ingest
          echo
          curl --fail-with-body -sS \
            -H 'content-type: application/json' \
            -d '{"op":"status"}' \
            https://uaosdkrfxidiwqljmelg.supabase.co/functions/v1/p37-stage-bright-v1-ingest
          echo
